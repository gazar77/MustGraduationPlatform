/**
 * Must match backend SiteSettingKeys.HeroBannerBgImages.
 * URLs come from admin site settings (typically `/uploads/...` on the API host); no bundled defaults.
 */
export const HERO_BANNER_BG_IMAGES_KEY = 'HeroBannerBgImages';
